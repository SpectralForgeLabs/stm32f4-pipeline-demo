name: STM32F407 Single Runner Build

on:
  push:
    branches: [ "hotfix/*" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: [spectral-single]
    steps:
      - name: Debug refs
        run: |
          echo "head_ref=${{ github.head_ref }}"
          echo "ref_name=${{ github.ref_name }}"
          echo "GITHUB_REF_NAME=${GITHUB_REF_NAME}"
    # steps:
    #   - uses: actions/checkout@v4 
    #   - name: Build firmware
    #     run: |
    #       docker run --rm \
    #         -e GITHUB_ACTIONS=true \
    #         -e GITHUB_SHA=${{ github.sha }} \
    #         -e GITHUB_REF_NAME=${{ github.ref_name }} \
    #         -e BUILD_NUMBER=${{ github.run_number }} \
    #         -v ${{ github.workspace }}:/home/runner/workspace \
    #         -w /home/runner/workspace \
    #         arm-none-eabi:15.2.single \
    #         bash -c "cmake -S . -B build -G Ninja \
    #           -DCMAKE_TOOLCHAIN_FILE=cmake/arm-none-eabi-gcc.cmake \
    #           && cmake --build build"

    #   - name: Upload artifacts
    #     uses: actions/upload-artifact@v4
    #     with:
    #       name: firmware-${{ github.sha }}
    #       path: |
    #         build/*.elf
    #         build/*.hex
    #         build/*.bin
    #       retention-days: 90

